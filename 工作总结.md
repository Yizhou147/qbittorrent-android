# 工作总结（2026-09-13）

本次会话目标：为 qbittorrent-android 适配 qBittorrent 4.3.9 和最新 5.x（选定 5.2.3），仅用 GitHub Actions 编译；建立独立自动化测试；过程中按用户反馈迭代修复。

## 一、最终状态总览

| 变体 | CI 构建 | 模拟器冒烟 | 真机实测 | 结论 |
|---|---|---|---|---|
| qb 4.3.9 (Qt 5.15.2 + libtorrent 1.2.20) | ✅ | ✅ | ✅ 可用 | **通过**（VueTorrent 为 v0.13.0 英文版，见下） |
| qb 4.6.7 (Qt 5.15.2 + libtorrent 2.0.10) | ✅ | ✅ | ✅ 可用 | **完全通过**（VueTorrent 2.34.0 中文正常） |
| qb 5.2.3 (Qt 6.6.3 + libtorrent 2.0.14) | ✅ | ❌ | ❌ 打开 WebUI 时闪退 | **未通过**（Qt6 运行时问题，见待办） |

最新可用 APK：
- 全变体构建 run：`34749801538`（tag `v1.2-rc2` 触发），Artifacts：`qbittorrent-android-qb4.3.9` / `qb4.6.7` / `qb5.2.3`
- 冒烟测试 run：`34750387234`
- 另有纯预构建（v1.1 原生库零改动）4.6.7 APK：run `34742475735`（build-apk-prebuilt.yml）

## 二、本次完成的工作

### 1. 多版本构建矩阵（已完成）
- 工作流 `build-android.yml`：`workflow_dispatch` 可选版本或 `all`；推 `v*` tag 触发全量
- qb/libtorrent 源码改从官方 release 下载，经 `ci/patches/<版本>/` 统一补丁集移植（共享库化 libqbt.so + JNI 桥接 + LinguistTools 可选化 + resumedatastorage 线程修复 + 5.2.3 rcc --no-zstd）；4.6.7 补丁结果与 v1.1 打包源码逐字节一致
- 版本参数：4.3.9→lt 1.2.20/Qt5/C++17/narrowing 降警告；4.6.7→lt 2.0.10/Qt5/C++17；5.2.3→lt 2.0.14/Qt6.6.3/C++20/STACKTRACE=OFF
- libtorrent 必须用 `git clone --recursive`（release tarball 缺 try_signal 子模块）
- libtorrent/libqbt 用 `ANDROID_PLATFORM=android-35` 编译（Android 16 TLS 对齐）

### 2. 构建架构重构（本次会话后半段的核心）
用户要求 Qt 编译独立成单独工作流并入库，最终架构：

- **`build-qt.yml` + `Dockerfile.qt`**（独立 Qt 构建）：源码编译 qtbase 5.15.2 / 6.6.3（带 v1.1 JNI 补丁：JNI_OnLoad 最小化只设 JavaVM、qjni.cpp 空指针守卫），产物（Qt 运行库 + openssl + qsqlite/TLS 插件）上传 artifact，人工下载后提交进仓库 `third-party/qt5|qt6/arm64-v8a/`。qt5 目前直接用的 v1.1 已验证库。
- **主构建 `build-android.yml` + `Dockerfile`**：不再编译 Qt；libqbt/libtorrent 用 aqt 预编译包的头文件/CMake 配置编译（同版本 ABI 兼容且 SSL 类完整）；运行时库从仓库 `third-party/` 拷入 jniLibs；依赖校验步骤对最终库集做 NEEDED 完备性检查。
- **`build-apk-prebuilt.yml`**（方案 A）：直接用仓库 jniLibs 里 v1.1 原生库打 APK，零编译，产出经真机验证可用的 4.6.7。

### 3. 自动化测试（`test.yml`，与构建分离）
- **patch-check**：三版本 vanilla 源码试打补丁并断言关键标记（已全绿）
- **unit-tests**：`QbtConfig` 纯 Java 类 + 12 个 JUnit 用例（配置写入/改写/端口校验），已全绿；曾抓出 v1.1 updateConfig 的追加逻辑缺陷（已修）
- **emulator-smoke**：Android 30 x86_64 模拟器（ARM 翻译）装 APK → `adb forward` → 带 `Host: localhost:8080` 头轮询 `/api/v2/app/version` 断言版本号；失败自动上传 logcat；轮询次数/间隔为手动输入参数（默认 20 次 ×10s）；三变体全覆盖
- 触发：push/PR 跑 1、2；构建成功后自动跑 3（也可手动指定 build_run_id）

### 4. 代码改进（随会话完成）
- Java 层：动态扫描加载 Qt5/Qt6 库；Service 双启动竞态修复；`QbtApi` 工具类去重三处 HTTP 代码；`QbtConfig` 抽取；废弃 `onBackPressed` 迁移；死代码清理；`nativeLibraryDir` 标准 API；JNI 桥接 TMPDIR 改可写目录
- README：多版本说明、新构建流程、技术问题记录（JNI_OnLoad/zstd/TLS 对齐等）、测试说明
- 版本号 1.2（versionCode 2）；gradle wrapper 改官方源

## 三、踩过的关键坑（已解决，备查）

1. **预编译 Qt 真机闪退**（本会话最重要教训）：aqt 预编译 Qt 的 `JNI_OnLoad` 会 RegisterNatives 激活依赖 Activity 的 Android 集成。打 jar 让它成功 → 运行期 `GetStaticMethodID(java_class==null)` abort（真机 4.6.7 WebUI 出现即闪退、5.2.3 未及 WebUI 闪退）。**正解 = v1.1 路线：源码重编 qtbase 打 JNI 补丁**
2. qt 源码编译环境差异（v1.1 本地脚本 vs Docker）：`-android-abis` 参数名、NDK r27 无 `aarch64-linux-android-ranlib`（补 `QMAKE_RANLIB=llvm-ranlib` 于 `default_pre.prf`）、`<limits>` 全量补丁、JDK 双版本（qt6 需 17、qt5 jar 需 8）、openssl 需 shared 且 qt5 android 链接名带 `_arm64-v8a` 后缀（建别名）、Dockerfile 步骤顺序（OpenSSL 必须在 Qt 前）
3. qb 4.x/5.x 的 `server.h` 无条件使用 QSsl 类 → Qt 必须带 SSL 编译（aqt 包恰好满足；自编路线用 shared openssl + `-openssl-linked`）
4. 模拟器冒烟 401 误判：qb 校验 Host 头，adb forward 后必须覆盖 `Host: localhost:8080`
5. GitHub API dispatch 偶发 500 → 用 `git tag` 触发构建作为替代

## 四、遗留问题（下次继续）

### 1. qb 5.2.3 闪退（最高优先级）
现象：构建/依赖校验全过，模拟器与真机均在 WebUI 就绪、WebView 打开瞬间 `JNI DETECTED ERROR: java_class == null`（GetStaticMethodID）→ SIGABRT。
分析：**Qt6 运行时库来自 `third-party/qt6/`（源码重编版），但 libqbt 是用 aqt 预编译包头文件编译的**。崩溃点在 WebUI/WebView 阶段（4.6.7 同架构却正常），疑似 Qt6Network 的 networkinformation/TLS 后端在运行期 dlopen/JNI 调用时缺类（Java 类未打包，且这次无法像 jar 方案那样补——会引入 Activity 依赖）。
下一步排查方向：
- 对比 qt6 自编库的 qsslsocket_android / qnetworkinformation 是否引用 `org.qtproject` Java 类（strings 检查）
- 尝试 qt6 configure 加 `-no-feature-networkinterface` 或裁剪 networkinformation 后端
- 或 qt6 运行时也改用 aqt 库 + jar（但真机已证明 jar 路线对 qt5 致崩，qt6 未必相同，可单独试）
- 模拟器 logcat 见 run `34750387234` 的 `emulator-logs-qb5.2.3` artifact

### 2. qb 4.3.9 的 VueTorrent 版本问题
现打包 v0.13.0（因 VueTorrent v1.0+ 要求 qb ≥ 4.4，其 API 依赖新版）。真机显示英文老界面、无中文。
可选方案（待用户拍板）：
- A. 维持 v0.13.0，README/界面标注"4.3.9 变体使用旧版 WebUI"
- B. 4.3.9 变体默认用 qb 官方 WebUI（默认已含中文翻译，资产里已有）
- C. 找 v0.13.x 系列里较新的版本（如 v0.13.1+）看是否改善

### 3. 工程收尾
- `test.yml` 的 emulator-smoke 仍是 `continue-on-error: true`，5.2.3 修复后应改为门禁
- 主构建 Dockerfile 中已不需要的 Boost/Qt 编译相关参数可再精简
- `scripts/` 下 v1.1 历史脚本与 `ci/` 并存，可整理归档
- tag `v1.2-rc1`/`v1.2-rc2` 是调试用的，正式发版前删除

## 五、关键命令速查

```bash
# 触发全变体构建（dispatch 不可用时用 tag）
gh workflow run build-android.yml -f qb_version=all
git tag -f v1.2-rcN && git push -f origin v1.2-rcN

# 触发冒烟测试（指定构建产物 + 轮询参数）
gh workflow run test.yml -f build_run_id=<run_id> -f poll_attempts=20 -f poll_interval=10

# 独立构建 Qt 运行时库（改动 Qt 补丁后需要，产物提交进 third-party/）
gh workflow run build-qt.yml -f qt_kind=both
gh run download <run_id> -n qt-runtime-libs-qt6 -D /tmp/qt6libs
cp /tmp/qt6libs/*.so third-party/qt6/arm64-v8a/

# 查看失败 job 日志
JOBID=$(gh run view <run_id> --json jobs --jq '.jobs[] | select(.name|contains("5.2.3")) | .databaseId')
gh api repos/Yizhou147/qbittorrent-android/actions/jobs/$JOBID/logs --allow-escape-sequences
```
